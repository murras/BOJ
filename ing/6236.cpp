#include <iostream>
#include <vector>
using namespace std;
#define MAX 1000000000
int main() {
    int n, m;
    cin >> n >> m;
    int low = 1;
    int k = MAX;
    vector<int> v;
    for (int i = 0; i < n; i++) {
        int input;
        cin >> input;
        v.push_back(input);
    }
    
}